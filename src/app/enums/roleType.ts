/**
 * Enum for Role Tyoe
 *
 * @readonly
 *
 * @enum {string} RoleType
 *
 * @property {string} ADMIN - The admin role
 * @property {string} USER - The user role
 */
enum RoleType {
  ADMIN = "admin",
  USER = "user",
}

export { RoleType };
