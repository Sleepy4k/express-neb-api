// import type { Request, Response, NextFunction } from 'express';

// const isGuestAreAdmin = (
//   req: Request,
//   res: Response,
//   next: NextFunction
// ) => {
//   if (req.session && req.session.user && req.session.user.role === 'admin') {
//     return next();
//   }

//   res.redirect('/login');
// }

// export default isGuestAreAdmin;
