/* eslint-disable perfectionist/sort-objects */
export const assetConfig = {
  dotfiles: "allow",
  etag: true,
  extensions: ["html", "htm"],
  index: false,
  maxAge: 604800000,
  cacheControl: true,
  immutable: true,
  redirect: true,
};
